program: "
assume (y == 10);
x := y + ??;
assert (x == 119);
"
input_output_examples:
  - input:
    output:
    vars:
pre_condition:
  condition: 
  condition_vars: 
post_condition:
  condition: 
  condition_vars: 
loop_inv:
  condition: 
  condition_vars:
