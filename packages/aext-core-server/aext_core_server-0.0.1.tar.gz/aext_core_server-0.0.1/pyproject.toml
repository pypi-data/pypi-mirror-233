[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aext-core-server"
dynamic = ["version"]
description = 'The aext-core-server component of anaconda-toolbox'
readme = "README.md"
license = "BSD-3-Clause"

[tool.hatch.version]
path = "src/aext_core_server/__init__.py"
