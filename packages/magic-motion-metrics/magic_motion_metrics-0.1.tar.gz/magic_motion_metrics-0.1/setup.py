from setuptools import setup, find_packages

setup(
    name='magic_motion_metrics',
    version='0.1',
    description='This package calculate metrics data using bvh motion data of a human',
    author='Muhammad Tariq ',
    author_email='tariq@movrs.com',
    packages=find_packages(),
    install_requires=[
        # this package does not require any other installations
    ],
)
