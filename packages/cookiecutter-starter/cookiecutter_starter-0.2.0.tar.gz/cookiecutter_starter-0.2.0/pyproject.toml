[tool.poetry]
name = "cookiecutter-starter"
version = "0.2.0"
description = ""
authors = ["Abderrahim Guennoune <aguennoune@outlook.com>"]
readme = "README.md"
packages = [{include = "cookiecutter_starter", from = "src"}]

[tool.poetry.scripts]
cookiecutter-starter = "cookiecutter_starter.main:app"

[tool.poetry.dependencies]
python = "^3.7"
typer = {extras = ["all"], version = "^0.9.0"}
click = "^8.1.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
