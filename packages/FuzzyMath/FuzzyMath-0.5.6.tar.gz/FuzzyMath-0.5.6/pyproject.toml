[build-system]
# Minimum requirements for the build system to execute.
requires = ["setuptools >= 43.0.0"] 
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=FuzzyMath --cov-report term-missing -x"
testpaths = [
    "tests"
]