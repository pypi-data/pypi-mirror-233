[tool.poetry]
name = "hypertune-sdk"
version = "0.1.0"
description = ""
authors = ["Jordan Webster <jordan@hypertune.com>"]
packages = [{include = "hypertune", from = "src"}]
include = ["src/hypertune/data"]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31.0"
cffi = "^1.15.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
