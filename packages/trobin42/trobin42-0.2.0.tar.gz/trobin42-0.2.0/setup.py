from setuptools import setup, find_packages

setup(
    name='trobin42',
    version='0.2.0',
    author='trobin42',
    author_email='trobin@student.42.fr',
    packages=find_packages(),
    install_requires=[
        # List dependencies here
    ],
)
