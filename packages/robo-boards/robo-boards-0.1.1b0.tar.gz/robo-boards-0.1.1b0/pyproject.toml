
# minimal when using setup.cfg

[build-system]
# Minimum requirements for the build system to run
requires = [
   "setuptools",
   "wheel",
   "numpy",
   "ntplib",
   "robo-base",
   "robo-time",]
   
   

    
  
