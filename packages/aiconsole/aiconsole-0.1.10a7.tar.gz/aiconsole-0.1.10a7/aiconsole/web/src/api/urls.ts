const BASE_URL = `http://${window.location.hostname}:8000`;

export const URLS = {
  EXECUTE: `${BASE_URL}/execute`,
  COMMAND_HISTORY: `${BASE_URL}/commands_history`,
  CHAT_HISTORY: `${BASE_URL}/chats_history`,
  ANALYSE: `${BASE_URL}/analyse`,
};
